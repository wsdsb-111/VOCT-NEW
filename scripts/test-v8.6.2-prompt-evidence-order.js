"use strict";
const assert = require("assert");
const fs = require("fs");
const fullSource = fs.readFileSync(require.resolve("../resources/app/out/main/prompts/prompt-builder"), "utf8");
const source = fullSource.slice(fullSource.lastIndexOf('case "history"'));
const v89 = source.slice(source.indexOf("if (options.v89Layout)"), source.indexOf("} else {", source.indexOf("if (options.v89Layout)")));
assert(v89.indexOf("appendPriorHistory();") < v89.indexOf("appendCurrentUser();"));
assert(v89.indexOf("appendCurrentUser();") < v89.indexOf("appendThirdPartyEvidence();"));
assert(v89.indexOf("appendThirdPartyEvidence();") < v89.indexOf("appendWorldTurnRecall();"));
console.log("V8.6.2 Prompt Evidence Order: PASS");
