"use strict";
const assert = require("assert");
const { buildKinshipGraph } = require("../resources/app/out/main/worldline/character-kinship-graph");
const { familyCharacters } = require("./v8.6.2-test-fixtures");
const graph = buildKinshipGraph(familyCharacters());
const deceasedSpouse = graph.relationBetween("1", "13").relation;
const survivingSpouse = graph.relationBetween("13", "1").relation;
assert.equal(deceasedSpouse.type, "DECEASED_SPOUSE_OF");
assert.equal(deceasedSpouse.label, "亡夫");
assert.equal(survivingSpouse.type, "SPOUSE_OF");
assert.equal(survivingSpouse.label, "妻子");
console.log("V8.6.2 Deceased Spouse: PASS");
