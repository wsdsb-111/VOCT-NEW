"use strict";
const assert = require("assert");
const { buildKinshipGraph } = require("../resources/app/out/main/worldline/character-kinship-graph");
const { familyCharacters } = require("./v8.6.2-test-fixtures");
const graph = buildKinshipGraph(familyCharacters());
const husband = graph.relationBetween("1", "13").relation;
const deceasedWife = graph.relationBetween("13", "1").relation;
assert.equal(husband.type, "SPOUSE_OF");
assert.equal(husband.label, "丈夫");
assert.equal(deceasedWife.type, "DECEASED_SPOUSE_OF");
assert.equal(deceasedWife.label, "亡妻");
console.log("V8.6.2 Deceased Spouse: PASS");
