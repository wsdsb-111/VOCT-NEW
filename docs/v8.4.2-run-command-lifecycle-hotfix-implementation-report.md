# V8.4.2 P0 Run Command 生命周期热修实施报告

## 1. 结论

本次按用户提供的 `v8.4.1-修复方案.md` 实施，但目标版本保持为 **V8.4.2**。修复对象是 `conversation_close` 迟到执行造成的 CK3 Talk Scene 闪退，以及 Run Command Queue 的队首阻塞；没有修改 Prompt、LLM Provider 或 Memory 语义。

确定性回归已通过，覆盖旧 Close rollover、TTL、ACK 超时、启动恢复、v2→v3 迁移、迟到 ACK、carrier 错配和普通 Action 的显式重试边界。本文不把自动化结果替代 CK3/VOTC 实机验收；重启后的世界线页面和连续 Talk Scene 仍需人工 smoke test。

## 2. 实施范围

### 2.1 Conversation 生命周期

- `ConversationManager` 使用单调递增 `conversationEpoch` 创建新会话。
- 新会话替换旧会话时，旧会话只进入异步 Memory Finalization，调用 `closeGameScene: false`，不再发送旧的 `mcc_event_v2.9002`。
- 正常结束会话仍发送 Close，并携带 `scopeId`、`epoch`、15 秒 TTL、`destructive` 和 `supersedable` 元数据。
- `RunFileManager.setCurrentConversationEpoch()` 会隔离并 quarantine 旧世代的 Close。

### 2.2 Run Command Queue v3

- 队列状态版本由 v2 升为 v3；旧状态启动时迁移，旧 `written` 状态规范化为 `awaiting_ack`。
- Close 默认 `expiresAt = queuedAt + 15000`，dispatch 前和 watchdog 中都会检查过期与世代。
- 未写出的 Close 启动恢复为 `expired`；已写出但 ACK 不确定的 Close 为 `quarantined`，均不会自动重放。
- Close ACK 超时立即 quarantine；普通 `action_effect` ACK 超时保留为 `stalled`，但必须由用户明确授权 retry，避免未知执行状态下重复 Effect。
- 被 quarantine/取消的 active command 会先按 command ID 校验并 neutralize `votc.txt`；carrier 属于其他 command 时拒绝清理。
- 迟到或乱序 ACK 只记录 `late_ack ignored`，不会改变当前队首或再次触发 dispatch。

### 2.3 可观测性

- 所有关键状态变更统一输出 `[RunCommand]` 日志，包含 command ID、kind、owner、scope、epoch、年龄、写入次数、前后状态和原因。
- `letters:getStatuses` 的 Date Tracker 状态中增加 `runCommandHealth`，包括 active command、队列年龄、stalled、expired、quarantined、HOL blocked 和 late ACK 计数；原有 BLOCKED/STALLED 操作入口保持不变。

## 3. Durable state 与 carrier 边界

`run-command-queue.json` 是唯一 durable source of truth；`run/votc.txt` 只作为当前 dispatch carrier。

启动恢复顺序由主进程保持为：扫描并 reconcile 已存在的 ACK、启动日志 tail、再次 reconcile、迁移/隔离未确认命令、清理匹配 carrier，最后才允许恢复有效的 queued command。任何有写入历史但没有 ACK 的命令都不会因为重启自动重写 carrier。

普通 Action 的安全合同仍是：

```text
写入历史 + 无 ACK → STALLED + carrier neutralize
                     ↓
              explicit retry 或 cancel
```

这会保留重复 Effect 风险提示，但不再允许 Close 以 stalled 状态永久卡住整个队列。

## 4. 变更文件

- `resources/app/out/main/actions/run-file-manager.js`
- `resources/app/out/main/conversation/conversation-manager.js`
- `resources/app/out/main/conversation/conversation.js`
- `resources/app/out/main/letters/letter-manager.js`
- `resources/app/out/main/main.js`
- `scripts/test-v8.4.2-run-command-lifecycle.js`
- `scripts/test-v7.10-rc6-rev2-run-command-queue.js`
- `scripts/test-v7.10-rc6-rev3-run-command-recovery.js`
- `scripts/test-manifest.js`

未修改 `%APPDATA%\\VOTC` 用户数据、CK3 存档、debug.log、API 配置或应用包版本字段；版本记录仍为 V8.4.2。

## 5. 自动化验证

已通过：

```text
node scripts/test-v8.4.2-run-command-lifecycle.js
node scripts/test-v7.10-rc6-rev2-run-command-queue.js
node scripts/test-v7.10-rc6-rev3-run-command-recovery.js
node scripts/test-test-manifest.js
node scripts/test-release.js
```

生命周期回归输出 `VOTC v8.4.2 Run Command Lifecycle: PASS`；发布门禁输出 `101 release groups` 的 PASS。测试使用临时目录和确定性时钟，不触碰真实 CK3 `votc.txt` 或用户数据。

## 6. 实机验收边界

重启 VOTC 与 CK3 后，建议连续创建/结束多个 Talk Scene，并观察：

1. 新 Talk Scene 出现后不会被旧会话延迟关闭；
2. Close 超时或重启恢复时，`votc.txt` 不保留旧 Close payload；
3. `debug.log` 可见 `[RunCommand]` 的 quarantine/neutralized/late_ack 诊断；
4. 信件、日期 Producer 和普通 Action 的原有 ACK/显式重试流程仍可用。

在上述 CK3/VOTC smoke test 完成前，本报告只声明代码与确定性回归通过，不提前声明真实 Scene 闪退 Gate 已完成。
