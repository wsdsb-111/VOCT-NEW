# V8.12 第二部分：历史检索与时间线

状态：**代码与自动化门禁完成，等待真实 CK3 / VOTC / GLM 两阶段实机 Gate。当前不得记录 `V8.12 PART 2 = PASS`，不得进入第三部分。**

前置条件 `V8.12 PART 1 = PASS` 已由用户确认。本阶段只把当前存档自己的 Temporal Archive 用于历史查询；普通 Current Query 继续走既有 Current World 路径。

## 已实现合同

- `AS_OF` 选择目标日期之前最近的有效节点，并明确返回实际 Checkpoint 日期；早于最早节点返回 `HISTORY_UNAVAILABLE`，未来日期返回 `HISTORY_FUTURE_BLOCKED`。
- `RANGE` 比较基线与范围内节点，只生成 Character、Title、War 的变化点。人物时间线覆盖地点、可靠头衔列表、领主、宫廷、生死、配偶与参战状态；朋友/宿敌因当前 Save Parser 没有可靠来源，显式报告未启用，不伪造支持。字段过滤在候选生成前生效，不会被 Top-K 先裁掉。
- Title 时间线覆盖获得/失去头衔、持有人、上级头衔与 Realm Root 变化。War 时间线覆盖活跃、首次出现、不再活跃与参战方变化；战争消失时只陈述“不再出现在活跃列表”，没有明确 `result` 时不推断胜负。
- WarActor 支持 `CHARACTER / TITLE / FACTION / UNKNOWN`。无法解析的一方仍保留为 opaque actor，并报告 `WAR_ACTOR_UNRESOLVED`，不会让整场战争消失。
- Historical Candidate 与 Current Candidate 分离，历史事实携带 Campaign、Branch、Checkpoint、`asOf`、有效期、来源与可见范围。历史 Canon 必须带日期或日期范围，并经过 Branch、ACL、Temporal 与冲突门禁；RANGE 按记录日期选择对应历史投影判断知情范围，不能用区间终点的新关系反推早年知情。
- 新增独立 Historical Scope：支持公开世界、公开领国、历史血亲、历史宫廷、个人记忆与公开战争；当前好友或当前配偶不能反推过去私密地点，无法证明时 fail closed。
- 历史缓存键包含 Campaign、Branch、Archive Revision、Checkpoint/Range、Query Plan、Responder、Policy 与 Canon Revision，不跨分支复用。
- 历史事实只进入 `worldline-turn-recall` Dynamic Tail。GLOBAL_STATIC、CONVERSATION_FROZEN、RESPONDER_FROZEN、Stable Kinship、Cache Anchor 与现有 Stable Prefix 不变。

## Review Fix：第二部分复核问题收口

- Temporal Archive `index.json` 新写入只保存 `characterCount`、`titleCount`、`warCount`，不再复制三组 ID；旧数组索引仍可读，并在下一次幂等归档时仅原子迁移 `index.json`。Gzip Checkpoint、CK3 存档、Memory 与 Canon 不被删除或改写；字符查询按日期命中索引后再懒读投影。
- Personal Memory 不再以“参与过叙事”泛化授权历史 `LOCATION`/`COURT_EMPLOYER`；只有带目标实体、字段和不晚于查询日期的结构化事实，或明确的地点直接观察，才能解锁私有历史字段。朋友/宿敌保留为未启用字段并返回 `HISTORY_FIELD_UNAVAILABLE`。
- 无明确人物的有界 `RANGE` 不再立即失败，范围收敛到玩家、玩家领地/查询领地、主要头衔、主要战争和高优先级 Canon；Character WarActor 同时桥接到 Realm，宋金等领地查询不会漏掉人物参战方。唯一解析的多人物查询不再被误判为身份歧义；真正的 Resolver ambiguity 仍 fail-closed。
- 不可靠 `primaryTitleId` 不再回退 `domainTitles[0]`；Prompt 使用“持有的头衔”，输出优先使用 Character/Title/Liege/Court/Spouse 可读名称并保留 Runtime ID。`HISTORY_INDEX_CORRUPT`、`HISTORY_CHECKPOINT_CORRUPT`、`HISTORY_CHECKPOINT_NOT_FOUND` 分开诊断，不把文件路径注入 NPC Prompt。
- Historical cache key 纳入 Realm refs、结构化记忆授权版本、紧凑索引布局版本和 Canon/Memory revision；历史事实仍只进入 Dynamic Tail，Stable Prefix、Current Query、Action、Memory 2.5 存储合同和 GLM 布局保持不变。

## 开关与回滚

| 设置 | 默认值 | 用途 |
| --- | --- | --- |
| `v812HistoricalRetrievalEnabled` | true | 允许历史检索与诊断预览 |
| `v812HistoricalPromptInjection` | false | 允许历史查询结果进入 NPC Prompt；实机 A 阶段保持关闭 |
| `v812HistoricalDiagnostics` | true | 在世界书开发者诊断显示检索、范围、裁剪、Scope、冲突与指纹 |

若回答异常，先关闭 Historical Prompt Injection，立即恢复 Part1 Current World 行为；若 Retriever 本身异常，再关闭 Historical Retrieval。Temporal Archive 可继续保留，不影响当前世界线。

## 自动化证据

- `scripts/test-v8.12-historical-retrieval.js`：除原有 AS_OF/RANGE、人物/头衔/战争时间线、WarActor、Scope、Historical Canon、Dynamic Tail 回归外，新增结构化 Memory ACL、直接观察、无实体有界 RANGE、多实体、Realm WarActor、可读名称、字段累积、候选头衔回退和 FRIEND 未启用诊断。
- `scripts/test-v8.12-temporal-archive.js`：除 gzip、不可变/幂等、隔离、重启、损坏节点与原子索引外，新增紧凑索引计数、旧数组索引兼容/原子迁移及 120 节点线性大小边界。
- `node scripts/test-release.js`：**309/309 发布组通过；378 个测试文件完成分类，68 个历史检查归档。**
- 主进程、Worldline、Renderer 语法检查通过；隔离 Electron 启动、世界书导航与 Renderer 错误检查通过。未连接真实 CK3 或收费 Provider；测试环境 Player2 未配置不属于 GLM 失败。

## 实机 Gate A：只看诊断，不注入 Prompt

保持 `Historical Prompt Injection = 关闭`，开启 Temporal Archive 与 Historical Retrieval，在世界书 → 开发者诊断依次测试：

- [ ] “岳飞五年前在哪里？”
- [ ] “岳飞五年前是什么头衔？”
- [ ] “1145到1150发生哪些变化？”
- [ ] “过去十年宋金有哪些战争？”

逐项确认日期、实际 Checkpoint、Campaign/Branch、人物唯一性、变化点、WarActor、Scope、裁剪与 Reason Code 正确。此阶段正式 NPC 回答仍走旧路径。

## 实机 Gate B：手动开启历史 Prompt

A 全部通过后，手动开启 `Historical Prompt Injection`，测试：

- [ ] “岳飞现在在哪里？”与“五年前在哪里？”能区分 Current / Historical。
- [ ] “这五年他的头衔变过吗？”只使用存档变化点。
- [ ] “1149年宋金是否在交战？”及“1150相比1149战争发生了什么变化？”不虚构胜负。
- [ ] 当前人物、当前战争、亲属、Memory、Action、Canon 与角色扮演行为无回归。
- [ ] 同一 NPC 按“当前 → AS_OF → RANGE → 当前”询问时 Stable Prefix Hash 不变，只有 Dynamic Tail 改变，并核对真实 GLM `cached_tokens`。
- [ ] 回档 Fork 后 A/B 分支的地点、头衔、婚姻、生死与战争互不串线；重启后结果一致。

只有以上真实 Gate 全部通过，才可记录 `V8.12 PART 2 = PASS`。随后必须暂停，并等待用户明确“第二部分实机测试通过，继续第三部分”。

## 实验记录：NPC 记忆作为历史头衔事件证据（第三阶段设计输入）

本次年度存档实验发现，年度 `autosave.ck3` 会在下一年度复用并覆盖旧文件。年度存档页面可以保留日期或路径状态，但不能据此恢复已经被覆盖的上一年度完整世界快照；切换到新的年度存档分支后，Temporal Archive 也可能从零开始。已被覆盖且未提前归档的 CK3 快照不能由当前文件反推恢复。

用户实机查看 NPC 记忆后发现，NPC 记忆会保留若干带日期的重大事件，并出现人物任职、授爵、头衔或官职变化等叙述。例如岳飞的记忆列表中存在带明确日期的任职与授爵相关记录。这说明 NPC 记忆可以作为“历史事件证据源”，用于验证某人物在某日期前后发生过头衔/官职变化；它不应被当作完整的年度世界快照或当前状态表。

对第三阶段的可行性判断：

- 可将 NPC 记忆按事件日期提取为追加式历史证据，并写入独立的 Temporal Archive 事件索引；年度 `autosave.ck3` 只作为当前状态和补充验证来源。
- 事件至少需要保留记忆持有者、事件主体、事件日期、观察日期、事件类型、头衔或官职标识、可读名称、原始记忆文本、来源检查点和置信度。相同事件被多个 NPC 记忆时必须去重。
- 历史查询只能使用不晚于目标日期的记忆事件；只有“获得/授予”而没有对应“失去/撤销”的证据时，不能直接推断该头衔后来持续保留。冲突或无法绑定稳定人物/头衔标识时应继续 fail closed。
- NPC 记忆是持有者视角的知情证据，且会随时间淡忘、只覆盖部分重大事件，可能存在重复、文本本地化差异和事件缺失。仅凭界面文本或 OCR 不足以建立可靠的历史索引，正式施工前必须确认原始存档字段能提供稳定 ID 和日期。
- 头衔变化可以优先由 NPC 记忆验证；地点、生死和完整当前头衔仍应优先使用 Current Runtime Truth 或结构化存档事实，不能由单条记忆泛化推断。

本实验结论：NPC 记忆适合作为第三阶段“历史头衔/官职变化事件索引”的候选底座，并能弥补年度存档覆盖造成的历史证据缺口；但截至本记录，尚未新增解析器、事件索引、持久化或 Prompt 接入，也未改变第二部分既有合同、Dynamic Tail 安排、实机 Gate 清单或 `V8.12 PART 2` 的未通过状态。上述内容只作为第三阶段方案设计依据。
