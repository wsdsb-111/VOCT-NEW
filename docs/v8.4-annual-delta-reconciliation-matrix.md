# V8.4 Luna + Terra：年度 Delta 对账矩阵

状态：已完成 S0→S1 的人物、Title、War 第一版对账矩阵；这是前置验证产物，不是正式 Delta Merger 或 Worldline Projector 的实现。

## 证据等级

- `CONFIRMED_BY_GAMESTATE`：两个 checkpoint 中存在直接、可重复读取的字段证据。
- `PARTIAL`：主事实已确认，但关系或事件时间仍需补充来源。
- `PROMOTED_TO_SUPPLEMENTAL`：GameState 不能充分重建，必须保留事件摘要或 delta。
- `RECONCILIATION_UNKNOWN`：当前样本不足以做可靠对账。

## 矩阵

| Delta 类别 | S0→S1 观察 | 当前结论 | 后续动作 |
| --- | --- | --- | --- |
| `CHARACTER_ALIVE_UPDATE` | 稳定 Runtime ID 的年龄、faith、location、positions 等字段可对比 | `CONFIRMED_BY_GAMESTATE` | 后续确定字段白名单与投影策略 |
| `CHARACTER_DIED` | 1,799 条 living→dead bucket 转移；死亡样本可读日期/原因 | `PARTIAL` | 补充事件顺序与死亡事件时间边界 |
| `NEW_CHARACTER/BIRTH` | 3,004 个 S1 新人物；其中玩家新子女 4 个，可读出生日期、dynasty、employer | `PARTIAL` | 父母关系当前未从新人物记录直接读出，不能仅凭 employer 强行替代亲子字段 |
| `HISTORICAL_FIGURE_BINDING` | 岳飞两个 Definition→Runtime 映射稳定；辛弃疾 Runtime 记录跨 checkpoint 稳定但 lookup 缺失 | `PARTIAL` | 完成 Definition Catalog 覆盖和 live A/B 样本 |
| `TITLE_HOLDER_CHANGED` | 1,186 条 holder 变化；286 条有 S1 窗口内 history，900 条仅 current holder | `PARTIAL` | 只有 history 明证的变更可 drop；current-only 变更保留 supplemental delta |
| `WAR_STARTED` | S1 新 active object 47 条；46 条 `start_date` 位于窗口内 | `CONFIRMED_BY_GAMESTATE` | 只对 start date 在窗口内且仍可读的 active war 删除同类 delta |
| `WAR_ENDED` | 67 条 S0 active → S1 无 active object；11 条仅存 raw event 引用，56 条无精确引用 | `PROMOTED_TO_SUPPLEMENTAL` | 保存结束摘要、双方/CB/目标与时间证据；不得由 S1 缺失自动补 result |
| `POSITION_CHANGED` | 玩家、岳飞等稳定人物的 positions 集合可见变化 | `CONFIRMED_BY_GAMESTATE` | Terra/Sol 决定哪些职位变化进入世界线事实 |
| `PLAYER_CANON` | 同一 `playthrough_id`、同一玩家 121091 | `PARTIAL` | 仍需 Save A/B、读档/继承和 branch identity 实机 Gate |
| `HISTORICAL_ID_EMPTY_SAFE` | 玩家与玩家子女的 `GetHistoryId` 为空且 Probe 正常返回 | `CONFIRMED_BY_GAMESTATE` | 保持空值为合法状态，不以姓名回填 |

## 当前禁止的结论

本矩阵不足以批准正式 V8.4 adapter、年度轮询、Delta Merger、Worldline 投影或 Prompt/Memory 接入。尤其不能将存档之间的数量变化直接解释为已捕获的游戏事件，也不能将 `character_lookup` 缺失解释为人物不存在或 History ID 为空；Title current-only 与 War ended 必须保留无法重建的事实，不得在 checkpoint 对账时静默丢弃。
