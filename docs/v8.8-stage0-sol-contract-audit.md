# V8.8 Stage 0 Sol 人物身份与亲属关系合同审计

## 结论

`V8.8 STAGE 0 SOL CONTRACT AUDIT = COMPLETE`

当前 V8.7.2 工作树具备可复用的 Definition/Runtime 双向索引、Runtime ID 亲属图、结构化死亡事实和 Character Knowledge/Secret 门禁，但尚未满足 V8.8 的统一人物关系合同。审计确认 `P0=1`、`P1=8`；在 Terra 完成以下阻断项前，不得进入 Luna Inspector，也不得签发 V8.8 Freeze。

本阶段只完成合同审计和交接文档，没有修改生产逻辑。审计对象是当前工作树，而不是设计文档所列的旧提交快照；工作树中既有 V8.7.2 未提交改动均保持原状。

## 冻结的权威边界

| 层 | 唯一职责 | 不得替代 |
| --- | --- | --- |
| Runtime Identity | 将本轮名称、显式 ID 或上下文指向 Runtime ID | 不得用历史年龄或家庭猜人 |
| Historical Identity | 以 Definition ID、精确规范名/批准别名和结构化性别确认历史身份 | 不得决定当前年龄、家庭、头衔、位置或生死 |
| Kinship | 以当前 CK3 Runtime ID 图确认关系和路径 | 不得让历史家庭覆盖当前家庭，也不得授予 Secret 权限 |
| Current State | 以当前 CK3 结构化事实决定生死、年龄、性别和位置 | Memory 与 Historical Baseline 只能补充过去或差异 |

所有事实必须以 Runtime ID 隔离。任何身份、关系、生死或性别冲突都必须返回结构化状态并停止精确事实投影。

## 当前实现中可以保留的基础

- `worldline/historical-identity-resolver.js` 的生产查询路径已经使用唯一 Definition ID、双向 Runtime 绑定、精确名称和结构化性别；年龄与家庭已退出该生产身份门禁。
- `worldline/character-kinship-graph.js` 的直接边统一为 `PARENT_OF`、`CHILD_OF`、`SIBLING_OF`、`SPOUSE_OF`，节点和边使用 Runtime ID，而不是姓名。
- `worldline/subjective-world-builder.js` 与 `character-knowledge-policy.js` 仍是权限权威；当前没有“亲属自动获得 Secret”的代码路径。
- Historical Definition Worker 查询缓存、Worldline 查询缓存和当前 Kinship WeakMap 缓存已有数量限制或随 Checkpoint/对象失效的基础，可在 Stage 6 上继续加固。

## P0 阻断项

### S0-P0-01：生死双重权威会向 Prompt 输出互相冲突的结论

`game-data/relationship-resolver.js` 的分层取值在 canonical `alive=true` 存在时忽略较低层的 `alive=false`，但仍会单独接收该较低层的 `deathDate`。由此可生成：

```text
alive=true
deathDate=1175年1月1日
evidence.conflicts.alive=false
```

随后 `character-family-facts.js` / `character-temporal-facts.js` 因存在死亡日期输出“已故”，而 `current-truth-adapter.js` 又可依据 `alive=true` 输出“在世”。这正是 V8.8 P0 Gate 中“死人被描述为当前活人”的可达冲突链。

Terra 必须先建立统一 Life Status 仲裁：

- `alive=true` 与任一可信死亡日期/死亡总天数同时存在时返回 `RELATION_LIFE_STATUS_CONFLICT`。
- 冲突时不得输出“在世”或“已故”的精确结论，也不得计算当前年龄或去世年龄。
- Current Truth、Family Fact 和 Prompt Projection 必须消费同一个仲裁结果，不得各自解释原始字段。

## P1 阻断项

### S0-P1-01：RelationMentionResolver 尚不存在

`shared-query-analyzer.js` 只能解析名称、Definition ID 和 Runtime ID。对“你儿子怎么样了”进行生产形状调用时，结果为 `resolved=[] / candidates=[] / NO_MATCH`；当前依赖把回应角色的整个家庭块交给模型自行理解，无法对多个儿子返回 `RELATION_AMBIGUOUS`。

### S0-P1-02：已故年龄仍会使用不可信 raw age

`character-age-service.js` 在缺少出生与死亡时间时，会把 `character.age` 作为 `ageAtDeath` 返回。该值可能是当前年龄或不明来源年龄，违反“已故年龄只冻结于 birth→death；证据不足则 AGE_UNKNOWN”的合同。

### S0-P1-03：Shadow Historical Resolver 仍以年龄和家庭参与身份判定

生产 Worldline 的 `historical-identity-resolver.js` 已满足新边界，但 `historical-system/historical-figure-resolver.js` 仍把年龄、家庭作为强身份分数，并把 `AGE_IMPOSSIBLE` 作为 hard conflict。该旧 Resolver 当前只服务 Dynamic History/诊断，不进入生产 Prompt，但 Luna 迁移 Inspector 前必须停用其身份裁决权或改接 Historical Resolver 2.0。

同时，`historical-data/figure-matching.js` 的 `resolverReady=true` 记录没有 Definition ID、Canonical Name 和 Production Mapping Source，不满足 V8.8 对 `resolverReady` 的新含义。

### S0-P1-04：缺少 branch-local HistoricalEntityBinding

当前绑定来自 Checkpoint 的 `definitionToRuntime/runtimeToDefinitions`，没有包含 campaign、branch、dataset revision、checkpoint 和状态的独立绑定合同。`getHistoricalBindings()` 只从单值 forward map 枚举，能够发现“一个 Runtime 对多个 Definition”，但不能完整展示“一个 Definition 对多个 Runtime”的冲突。

### S0-P1-05：性别来源冲突没有统一状态

`character-demographic-normalizer.js` 采用首个可用值并静默忽略后续来源；`relationship-resolver.js` 只检查被选优先级内部的冲突。Current Character 与 relation entry 冲突时可能保留一个值而不产生 `RELATION_GENDER_CONFLICT`。

### S0-P1-06：身份、关系状态和路径 DTO 尚未统一

现有身份路径混用 `RESOLVED/REJECTED/NO_MATCH`、`DIRECT/LIVE_CONFIRMED/CONFLICT` 等状态；关系冲突多以 `null + diagnostic` 表示。Kinship `relationPath` 目前只是 ID 数组，缺少每一步 `type/from/to`，也没有 Biological/Adoptive/Step 分类。

### S0-P1-07：Family Prompt 仍是全量、非 query-aware 投影

`buildFamilyFactBlock()` 每轮遍历并输出回应角色全部可展示亲属。它没有 Relation Query、目标 Runtime ID、歧义状态和 Top-K 投影边界，不符合“Retrieve, Don't Dump”。

### S0-P1-08：Historical/Kinship 联合缓存合同尚未建立

现有 Runtime Name、Historical Definition、Kinship 和 Worldline 查询缓存彼此独立。尚无统一包含 `campaignId + branchId + checkpoint structural revision + historical dataset revision` 的 Binding/Relation 缓存键，也没有 Relation Mention cache 的 Load Boundary 失效合同。

## Terra 实施顺序与验收门

1. 先修 `S0-P0-01`，新增生产形状的 alive/death 冲突测试；该测试通过前不得扩展 Prompt。
2. 冻结统一的 Entity Resolution、HistoricalEntityBinding、Relation Resolution、Relation Path 和 FamilyEntityFactBundle DTO；所有记录只按 Runtime ID 合并。
3. 完成 Runtime 同名与上下文消歧；给名、同名和候选集不完整必须 fail-closed。
4. 将 Historical Resolver 收敛到 Definition ID + exact approved name/alias + structured gender；年龄、家庭和现实死亡年只生成 Worldline Difference。
5. 实现 RelationMentionResolver 与 FamilyEntityFactBundle，覆盖单一/多个/同名儿子、祖孙路径、生死、ageAtDeath 和 gender conflict。
6. 将 Family/Entity 事实先经过 Knowledge Policy/ACL，再进行 query-aware 投影；Kinship 只能提升相关性，不能提高权限。
7. 最后接入 branch-local binding、Load Boundary 失效、bounded cache、serialization、integrity scan 和 profiling。

Terra 必须优先建立这些硬回归：

```text
alive=true + deathDate => RELATION_LIFE_STATUS_CONFLICT，不输出精确生死
dead + 无 birth/death => AGE_UNKNOWN，不使用 raw ageAtDeath
“你儿子” + 单一儿子 => 唯一 Runtime ID
“你儿子” + 多个儿子 => RELATION_AMBIGUOUS
同名儿子一死一活 => 两套事实按 Runtime ID 完全隔离
Historical age/family divergence => 身份仍由 Definition ID 决定
Historical gender conflict => IDENTITY_CONFLICT_GENDER
亲属关系存在 + Secret ACL 不允许 => 不召回 Secret 正文
Load/branch 切换 => Binding、Kinship、Relation Mention 缓存全部失效
```

## 已执行验证

- V8.5.2 Historical Identity core gate 与 V8.6 Historical source conflict 回归通过。
- V8.6.2 relation type conflict、age-at-death、sex label、family prompt 回归通过。
- V8.7.0 production family adapter、Kinship cache 与 structural revision 回归通过。
- 当前完整发布基线：`249/249` 发布组通过，318 个测试文件已分类；本阶段未修改生产代码，未重复签发实机 Gate。
- 额外只读复现确认了：Relation query 未解析、dead raw-age fallback，以及 `alive=true + deathDate + conflict=false` 的 P0 链。

## Stage 0 状态

Stage 0 Sol 审计已完成，可以切换 Terra 开始 Stage 1—4。当前状态：

```text
V8.8 STAGE 0 = AUDIT COMPLETE
V8.8 P0 = 1 OPEN
V8.8 P1 = 8 OPEN
V8.8 FREEZE = NOT READY
```
