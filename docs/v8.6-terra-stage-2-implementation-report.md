# V8.6 Terra Stage 2：Runtime Index / Worker Recovery / Base Discovery

日期：2026-09-06  
状态：`PASS（自动回归）`；V8.6 总冻结仍为 `NOT READY`

## 交付

- 新增 `worldline/runtime-name-index.js`。Checkpoint 的 `firstName` 继续只构成给名候选；完整姓名只在 Live 数据与 Checkpoint 的游戏日期、玩家 ID、角色 ID 和给名均相符时进入 `verifiedFullNameToRuntimeIds`。索引记录 `LIVE_FULL_NAME` provenance，保留同名多候选，不写回原始 Checkpoint。
- 新增 `worldline/runtime-definition-index.js`。使用 `WeakMap` 对一个 Snapshot 一次性构建 Definition → Runtime 与 Runtime → Definition 联合索引；历史/原生身份判断不再在查询热路径反复扫描整个 forward map。
- `WorldlineService` 在 CK3 日志成功解析后刷新可信 Live 名称索引；存档解析和持久化保持原 schema，索引仅附着于进程内当前 Checkpoint。旧存档缺此索引时安全降级为给名候选。
- Historical Definition Worker 改为单 Worker、`FAILED_TRANSIENT` 的 1 秒 / 5 秒 / 30 秒有限自动恢复。三次预算耗尽后为 `FAILED_STABLE`，不因查询重启；显式刷新、路径变化或 dispose 分别重置或取消恢复。短暂 READY 后立即崩溃不清空失败预算。
- Base Game Discovery 2.0：显式路径、Steam `libraryfolders.vdf`、已知可执行路径、Mod 推断、手工回退按顺序验证。零 Mod 可用；多个未消歧安装返回 `SELECTION_REQUIRED`，不扫描磁盘。周期 Probe 使用已解析来源及文件 `stat`，仅在配置或 descriptor 变化时重新发现。

## 验证

- `test-v8.6-runtime-full-name.js`：同 scope 的 Live 完整姓名、provenance、同名、旧 Snapshot 与不可变索引。
- `test-v8.6-definition-reverse-index.js`：正反映射联合、WeakMap 重用与无 Snapshot 变异。
- `test-v8.6-worker-recovery.js`：1/5/30 秒、单飞行、稳定失败、显式新 episode、dispose 取消。
- `test-v8.6-base-game-discovery.js`：配置路径、Steam 元数据、可执行文件、手工回退和多安装歧义。
- 保留的 `test-v8.5.2-historical-lifecycle.js` 覆盖 descriptor/source-set 改动、无变更循环和迟到 Worker 回复。

本阶段未启动 CK3、Electron 或 Provider；没有声称真实游戏安装、UI 路径选择或多人 Prompt 隔离已验收。下一阶段 Terra Stage 3 负责共享候选、字段级 Knowledge Policy、Scope/Memory adapter、主观 DTO 与 Prompt Phase A。
