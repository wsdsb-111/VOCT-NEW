# V8.5.1 Sol 最终审查与修复记录

日期：2026-09-04。依据：用户提供的 V8.5.1 历史人物通用召回与诊断可读性方案，以及 [Sol 合同](v8.5.1-sol-contract-freeze.md)。保留 Terra/Luna 已有改动，本轮不提交 Git、不修改游戏存档或用户配置。

**本轮审查与修复已完成；自动回归通过。V8.5.1 INTERNAL FREEZE = NOT READY。** 本版不调用 Astra，Prompt Default On 保持关闭。以下明确区分实现验证、只读真实存档验证和未完成的实机验收。

## 1. 已修复的问题

| 优先级 | 问题及影响 | 修复与验证 |
| --- | --- | --- |
| P0 | 同一 Definition 指向多个 Runtime 时可能按年龄选出一个；历史拒绝结果可被记忆实体提示或本地化回退重新引入事实 | 多 Runtime 绑定硬门禁，正反绑定一致性检查，curated 与源生日/性别冲突拒绝；按实体阻断旁路，保留其他明确查询对象。Sol safety / identity / matrix 回归通过 |
| P1 | 本地化正则的转义与普通字符分支重叠；未闭合引号加连续反斜杠会发生灾难性回溯。真实源首次构建曾超过 120 秒 | 两套本地化解析器排除普通分支中的反斜杠；加入 40 个反斜杠的异常行回归。修复后真实索引构建与存档解析合计约 5.2 秒，不代表已证明所有历史白屏均来自这一问题 |
| P1 | 历史定义按行/整块正则提取，嵌套效果或注释可能污染姓名；无关日期块内畸形字段导致整体构建失败 | 复用直接条目扫描器，只读身份字段和出生日期块；无大括号的说明文件跳过，解析错误显式标记不完整。未修改 Mod 文件 |
| P1 | Worker 旧世代结果、失败及超时可能留下旧缓存或悬挂请求；来源变动不及时反映 | Worker 所有消息绑定实例，取消时结清请求，查询缓存有界，初次及周期构建均有超时；内容/描述符/策略参与 revision，后台约 60 秒重新核验，重新读取存档触发失效 |
| P1 | 异步诊断等待后反复执行完整 Analyzer；pending 结果可能污染 Topic cache | 准备数据后只执行一次最终分析，等待中 checkpoint 改变则拒绝旧结果；pending 分析不进入 Topic cache |
| P1 | 通用姓名能被诊断召回，但历史映射页仍只查 curated 名称；玩家赵思昭被历史前缀赵思抢占 | 历史映射 IPC 有界准备通用索引，映射筛选使用命中的 Definition；独立姓名只匹配完整姓名，当前玩家仍能进入 Game Truth |
| P1 | 默认展示泄漏 Fei_name11、参与者 (#ID)，说明层和原始层顺序错误，候选详情可能无界 | A/B 层过滤 raw key/ID、解释 evidence 与拒绝原因；B 在 C 前；C 懒展开并分页保留原始候选、证据、queryPlan、coverage trace，每页最多 50 条 |
| P1 | 相对 Mod path 在解析前检查存在性；无文化证据也机械拼接中文姓氏 | 先相对 CK3 用户目录解析路径，再验证；组合姓名要求明确 han/chinese 文化、单字姓和完整静态链；house/culture 变化纳入冲突判断 |

索引策略版本为 `v8.5.1-historical-index-3`。`constructor` 等原型属性不再能成为索引命中。索引查询最多回传 200 个 Definition；候选集合被裁剪时不能确认身份。Reverse binding 索引按 snapshot 对象缓存，避免每位候选重复扫描约九万条绑定。

## 2. 真实存档只读矩阵

测试输入为用户指定的 `autosave_1.ck3`，游戏日期 `1169.4.7`，解析到 156,848 个角色。当前发现的来源为基础游戏与 54 个启用 Mod，127,088 个 Definition、45,734 个可搜索全名键、5,167 个带冲突记录。这里的 READY 表示当前实现的源读取检查通过，不等于所有 Launcher 覆盖语义都已证明与游戏一致。

调用路径：后台源索引 → Shared Query Analyzer → 身份候选/事实分离 → 历史映射筛选。脚本使用临时独立数据目录，没有改写实际 checkpoint、补充知识或存档。

| 姓名 | Definition 数 | Runtime 候选数 | 本存档结果 | 历史映射行数 |
| --- | ---: | ---: | --- | ---: |
| 岳飞 | 2 | 2 | 歧义，两个候选均 0.77 | 2 |
| 韩世忠 | 2 | 1 | 证据不足，0.40 | 2 |
| 秦桧 | 2 | 2 | 歧义 | 2 |
| 赵构 | 2 | 1 | 证据不足 | 1 |
| 吴玠 | 2 | 1 | 证据不足 | 1 |
| 宗泽 | 2 | 1 | 证据不足 | 2 |
| 张浚 | 3 | 1 | 证据不足 | 2 |
| 张俊 | 4 | 3 | 歧义 | 3 |
| 李纲 | 6 | 4 | 歧义 | 5 |
| 辛弃疾 | 2 | 0 | 有历史定义，当前存档无绑定角色 | 0 |
| 范仲淹 | 2 | 1 | 证据不足 | 1 |
| 王安石 | 2 | 1 | 证据不足 | 1 |
| 苏轼 | 2 | 1 | 证据不足 | 1 |
| 文天祥 | 1 | 0 | 有历史定义，当前存档无绑定角色 | 0 |
| 赵匡胤 | 3 | 2 | 来源冲突/证据不足，未确认 | 2 |

15/15 指的是本样本的 **Definition 召回**，不是身份确认率，也不是全索引 ≥95% 准确率的证明。所有未确认历史候选均未进入 Game Truth。未降低 0.85 确认阈值或 0.15 唯一性间隔；未知性别不按男性加分。

玩家“赵思昭”不在历史姓名索引中，但当前存档人物事实命中 Runtime `121091`。历史索引未收录与当前角色命中是两种独立状态，默认摘要以已找到的存档事实为准。映射行数不是唯一 Runtime 人数。

## 3. 验证结果与复现

- `node scripts/test-release.js`：最后一次完整运行通过 **109 groups**；178 个测试文件已分类，68 个历史检查归档。归档分类不表示本次逐一运行了这些历史检查。
- `node --check resources/app/out/main/main.js` 及 release syntax gate：通过。
- 新增 `scripts/test-v8.5.1-sol-safety.js`：解析边界、源文化、绑定冲突、记忆回退、玩家姓名前缀、单次 Analyzer、checkpoint 换代、Worker 迟到/失败、玩家层 ID 隐藏。
- 既有 index / worker / 100-query matrix 全通过。修正 fixture：明确声明中文文化；别名只有 0.75 分时应召回候选而非事实；普通本地化回退用非历史姓名，另测历史姓名不能绕过身份门禁。
- `scripts/v8.5.1-real-checkpoint-audit.js <save> <CK3 user folder>`：上述 15 人和玩家的真实只读测试通过；最后一次语义修改后，构建加解析 5,223ms，单次分析加映射筛选约 109–163ms。160 次热缓存 `find()` 中位约 0.0004ms，P95 约 0.0007ms，最大约 0.012ms；这只是进程内 Map 命中，不能当成端到端 IPC 或冷查询延迟。
- 使用 Playwright 技能及仓库已有 `scripts/v8.5-sol-ui-smoke.js`，加载实际 Renderer/helper，IPC 使用隔离 fixture。1024/1280/1440 × parchment/ink/knight 九组合交互通过；42,313 条旧裁剪数据展开最多 50 行，392 个 DOM 节点，无 pageerror；A/B 不泄漏测试中的 raw key、Definition/Runtime ID，C 仍可访问原始信息。
- 浏览器脚本需要本机 Playwright 模块与 Edge，可通过 `VOTC_PLAYWRIGHT_MODULE`、`VOTC_BROWSER_PATH` 指定。输出截图位于 `output/playwright/v8.5.1-diagnostics.png`，不包含真实存档数据。

## 4. 未冻结项与实现边界

1. **实际 Electron UI Gate 未验收**。隔离浏览器未加载完整应用外壳、真实背景资源和 IPC，不证明实际 VOTC 不再白屏，也不证明三个主题的最终色彩/对比度达标。
2. **全量 Exact-name Recall Gate 未验收**。100 条 fixture 与 15 人真实样本分别验证逻辑和固定名单，不能替代独立人工标注的完整来源抽样；不宣称全索引 ≥95%。
3. **来源合同尚有缺口**。Base 仍主要由 Workshop 所在 Steam 库推导；仅本地 Mod、无启用 Mod 或跨库安装可能无法发现 Base，此时 fail-closed。压缩 Mod / 相关 replace_path 标记不完整，但同路径覆盖、Launcher 实际加载状态及完整 name→house/dynasty→localization 逐节点来源证明尚未完整建模。当前索引读取完整性不能作为这些语义已证明的结论，相关来源需继续核验后才冻结。
4. **索引为内存缓存**，尚无独立磁盘索引缓存；周期重建在 Worker 运行而不是每 Turn 同步扫描，但未完成长期运行 CPU/内存和持续来源切换的压力验收。Main 历史映射筛选仍有全绑定集合遍历，实测百毫秒级，不应误报所有查询都在 5ms 内。
5. **增强资料状态仍不完整**：未将所有缺失 curated 资料情形独立暴露为完整的正交 metadata coverage；通用 Definition 候选召回已不依赖 curated 白名单。未为扩大确认率臆造性别、生日或别名资料。
6. 真实 Provider/CK3 对话、长期混合查询及 V8.5 既有人工 Gate 不因本轮离线回归被追认通过。Prompt 基础默认保持关闭。

因此 Sol 给出的是“已修复项自动回归通过，冻结阻断项明确”，不是无条件发布批准。尚未完整满足来源合同的实现不能仅凭 UI 验收后自动转为 INTERNAL FREEZE PASS。

## 5. 用户下一步验收

完全退出 VOTC 后重新启动，让主进程、Worker 和 Renderer 都加载新代码（本轮未擅自终止用户程序）。选择同一存档，等待来源读取结束：

1. 历史人物查“岳飞”“韩世忠”，应显示映射或明确候选/来源状态，而非无反馈。
2. 诊断查“岳飞”：不能随意确认两个同分候选；展开判定依据与开发者数据、连续翻页，再切回概览。
3. 诊断查“赵思昭”：当前存档应命中玩家事实；历史索引未收录不应覆盖默认摘要里的当前角色结论。
4. 切换三个主题、调整窗口宽度、反复进入世界线；记录白屏/无响应是否复现及所查姓名。来源变动或读取中应有明确的不完整状态，不应给确定否定。

无需切 Astra。实际反馈与剩余来源合同核验完成前维持 NOT READY。
