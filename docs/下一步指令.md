Phase 0.6 完成后，不再进行任何代码修改。

现在生成本次 v6.5 动作系统重构的最终工程交接文档：

docs/v6.5-terra-handoff.md

这份文档的目标读者是后续接手核心架构重构的 GPT-5.6 Terra / Codex。

要求：
不要重新长篇复制 Phase 0 / 0.5 / 0.6 报告。
这是一份“总入口 + 施工交接”文档。

必须包含：

1. 当前版本关系

v6.4
= 当前生产基线

v6.5
= Event-based / metadata-driven 动作系统架构重构

2. 当前已经完成的工作

- 架构设计文档
- Action System Inventory
- Phase 0 Mixed-Semantic Regression Tests
- Phase 0.5 Architecture Boundary Tests
- Phase 0.6 Runtime/Test Classification 工作
- 高风险 Action run() 测试

3. 明确没有修改的内容

特别说明当前尚未实施：

- Candidate Gate 重构
- ActionEvent
- Execution Parser
- Positive Evidence runtime pipeline
- Registry-driven Semantic Resolver
- semantic metadata runtime
- Player/NPC runtime 修复
- Event-level dedupe
- Legacy semantic 删除

4. 当前已确认的真实架构问题

至少包括：

A. Stage 2 full-text contamination
getSemanticActionProfile 重新扫描完整原文，
导致被否定的死亡、罢免、离开等语义重新进入 allowedActionIds。

B. Gate false positive / false negative
包括 hypothetical death false positive
以及：
“我现在把50金币交给你”
的 gold Gate false negative。

C. pendingPlayerActionMessage 问题
明确记录当前生产路径：

pendingPlayerActionMessage ?? placeholder

可能导致第一条 NPC reply 自己的动作没有被独立评估。

D. message-level semantic 无法表达真正 Event 边界

包括：
- 多 Event
- 同类多个 Event
- Event 顺序
- Event-level dedupe
- execution vs result
- failed-before-execution vs executed-but-result-failed

5. 当前测试状态

不要只写一个总通过率。

分别说明：

- 原有 v6.4 tests
- Mixed Semantic tests
- Known v6.4 failures
- Boundary tests
- Runtime testability gaps
- Gold Gate coverage failure
- high-risk run() tests

并说明：

KNOWN_V6.4_FAILURE
是未来 v6.5 必须转成普通 PASS 的验收项。

6. v6.5 Acceptance Checklist

至少包括：

- 否定死亡 + 实际受伤
- 否定罢免 + 实际骑士任命
- 否定离开 + 实际进入
- 否定恋人 + 实际朋友
- hypothetical death 不进入 Positive Event
- gold “把50金币交给你” 能识别
- Player message 独立评估
- NPC reply 独立评估
- 一条 message 多个 ActionEvents
- 每个 Event 独立 evidence span
- executionStatus / resultStatus 分离
- attempted-before-execution 与 executed-but-missed 分离
- Event 顺序保持
- 同 category 不同 target 不被 dedupe
- Stage 2 不扫描完整原文
- high-risk Action fail-closed

7. 推荐 Terra 的施工顺序

建议：

Phase 1:
确认现有测试与代码真实状态

Phase 2:
定义 ActionEvent / Execution Parser 接口

Phase 3:
实现 Positive Evidence 隔离
优先修复：
否定死亡 / 罢免 / 离开三个 P0 failure

Phase 4:
修复 Player/NPC 独立 evaluation

Phase 5:
建立 semantic metadata schema +
Registry-driven Semantic Resolver

Phase 6:
先迁移：
characterIsKilled
isInjured
isImprisonedBy

Phase 7:
逐步迁移其他 Action

Phase 8:
Candidate Gate 降权、高召回化

Phase 9:
Legacy fallback 删除

不要要求 Terra 机械遵循此顺序。
如果 Terra 审查代码后认为需要调整，允许重新规划。

8. Terra 必须重点审查的位置

- getActionTriggers
- getSemanticActionProfile
- getActionIdsForTriggers
- shouldEvaluateForMessage
- evaluateForCharacter
- pendingPlayerActionMessage
- actionGateProcessedTriggers
- Action Registry
- ActionPromptBuilder
- scripts/test-action-system.js

9. 文档索引

列出并说明用途：

- VOCT_v6.5_动作模组架构改进与语义系统重构设计.md
- docs/action-system-inventory.md
- Phase 0 report
- Phase 0.5 report
- Phase 0.6 report
- scripts/test-action-system.js

10. 最后给 Terra 一段明确交接语

说明：

当前测试准备阶段已经完成。
请先审查当前代码、设计文档、handoff 和 regression tests，
不要直接假定前一模型的架构判断全部正确。

以真实代码和测试结果为最终依据。

优先解决已被 regression tests 证明存在的 bug，
而不是为了形式上的“Event 架构”进行大规模重写。

高风险状态变化必须 fail-closed。

完成这份文档以后立即停止。

不要：
- 修改任何生产代码
- 修改测试
- 添加 metadata
- 实现 ActionEvent
- 修改 Gate
- 删除 Legacy
- git push

最后输出：
- handoff 文件路径
- 当前 git diff / working tree 中有哪些修改
- 哪些修改仅属于测试和文档
- 是否存在任何生产代码修改