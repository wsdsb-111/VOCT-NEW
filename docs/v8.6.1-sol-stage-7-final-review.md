# V8.6.1 Sol Stage 7 最终审查

日期：2026-09-07

## 结论

`SOL STAGE 7 CODE REVIEW = PASS`，未发现遗留 P0。Sol 终审发现并修复一项 P1 诊断证据问题：旧版对话记录没有 V8.6.1 分块观测字段时，世界线诊断页不再把它当作当前版本记录展示。

采纳用户提供的实机时序说明：本次实机对话发生在 Terra 主程序链路完成后，因此可记为 `TERRA MAIN-LINE LIVE SMOKE = PASS`，不因 Luna 前端随后更新而作废。现存用量记录也确认该窗口真实完成了 Provider 调用。

原实机记录早于 Sol Stage 4 的后置主进程修复与 V8.6.1 指标持久化。随后已完全重启最终版并补做实机对话，新生成 7 条带完整 V8.6.1 分块字段的 Chat 记录，最终版观测管线 Gate 通过。用户于 2026-09-07 确认当前实机没有问题，并明确豁免剩余生产 Worldline 10/30/50 A/B；该矩阵不记为已执行，但不再阻断本版本冻结。最终结论为 `V8.6.1 FULL FREEZE = PASS (USER ACCEPTANCE)`。

## Sol 修复

- 世界线 Token / Cache 面板现在只接受同时含 `memoryStableTokens` 与 `worldTurnRecallTokens` 的 V8.6.1 对话记录。
- 旧记录仍保留在原用量统计中，但不会以一组零值或破折号冒充 V8.6.1 实机观测。
- 没有合格记录时，面板明确提示完成当前版本对话后刷新。
- 新增回归断言，锁定旧记录与 V8.6.1 观测记录的来源边界；未改变 Prompt、Memory、Worldline 或 Provider 请求行为。

## 实机证据审计

本轮只读取本机持久化用量元数据，不读取或输出 Prompt 与对话正文。

| 检查项 | 证据 | 结论 |
| --- | --- | --- |
| Terra 主链路实机 | 2026-09-07 00:10:38–00:17:01，共 11 次 `deepseek-v4-flash` Chat 请求 | PASS |
| Provider 用量 | 输入 122,634 Token；输出 3,591 Token | PASS |
| Provider Cache | 命中 102,912；未命中 19,722；命中率 83.9% | PASS（仅该实机窗口） |
| 最终版 V8.6.1 实机 | 2026-09-07 01:30:05–01:35:23，共 7 次带新字段的 `deepseek-v4-flash` Chat 请求 | PASS |
| 最终版 Provider 用量 | 输入 78,339；输出 3,529；缓存命中 56,832；未命中 21,507；命中率 72.5% | PASS（A 组短窗口） |
| V8.6.1 分块观测 | 7 条新记录均可识别；最新记录包含 Stable/Dynamic、Memory 分块、缓存层与耗时 | PASS |
| 当前生产开关 | `subjectiveWorldMode=DIAGNOSTIC`、`promptIntegrationEnabled=false`；7 条记录 Worldline Token 均为 0 | A 组符合预期，B 组未执行 |
| 10/30/50 A/B | A 组为 7/10 最小检查点，B 组为 0 | 未执行部分由用户明确豁免 |
| V8.6.1 冻结 | 代码门禁、Terra 主链路与最终版观测通过；用户确认实机无问题 | PASS（人工验收） |

现有配置值只证明审查时的保存状态；旧遥测没有记录当时的两个开关，因此不反推用户首次实机时是否曾临时切换。补测窗口明确对应当前默认 A 组，证明最终版主进程、Provider 与观测字段已接通。生产 Worldline B 组仍属于未执行证据，本次通过来自用户对剩余矩阵的明确豁免，不把豁免写成测试通过。

## 自动化验证

- 14 个 `test-v8.6.1-*` 专项全部通过。
- `node scripts\test-release.js` 通过：149 个发布组、218 个测试文件分类、68 个历史检查归档。
- Renderer、Conversation、Usage Analytics、PromptBuilder 与 Worldline Service 语法检查通过。
- `git diff --check` 无补丁错误，仅有工作区既有 LF/CRLF 转换提示。

## 冻结签发与保留边界

- 最终版完全重启、V8.6.1 分块观测、Terra 主链路和 Provider 实机均已有证据。
- 用户确认当前没有问题，并明确要求直接通过；剩余 A 组 10/30/50 长窗口与生产 B 组不再作为 V8.6.1 发布阻断项。
- 默认配置继续保持 `subjectiveWorldMode=DIAGNOSTIC`、`promptIntegrationEnabled=false`，本次冻结不会自动开启生产 Worldline。
- 后续若计划默认启用生产 Worldline，仍建议使用同存档、同角色、同脚本、同模型与同 Provider 补做 10/30/50 A/B，并核对 Presence、Secret、摘要召回和失败隔离；该建议不改变本次 `PASS`。

`SOL STAGE 7 CODE REVIEW = PASS`

`V8.6.1 FULL FREEZE = PASS (USER ACCEPTANCE)`
