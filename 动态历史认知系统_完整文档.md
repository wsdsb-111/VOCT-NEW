# 动态历史认知系统 - 完整使用文档

## 📋 系统概述

这是一个智能的历史背景系统，能够根据游戏中的时间动态提供历史背景信息，同时处理"游戏剧情"与"真实历史知识"之间的关系。

### 核心设计理念

1. **游戏数据优先**：当前游戏中的皇帝、官员等信息从实际游戏数据动态获取
2. **历史知识背景**：真实历史人物和事件作为"历史知识"存在，不会因游戏剧情改变而消失
3. **认知边界**：角色只能知道当前年份之前的历史，未来对他们是未知的
4. **玩家自由度**：支持玩家篡位、改变历史等沙盒玩法

### 适用场景

- 玩家正常游玩，NPC按真实历史背景对话
- 玩家篡位做皇帝，NPC仍认识范仲淹、岳飞等历史人物
- 不同年代进行游戏，AI自动适配时代背景
- 防止AI使用"上帝视角"预知未来

---

## 🏗️ 系统架构

### 1. GameData 类新增属性

```javascript
class GameData {
  // 时间相关
  year: number;              // 当前年份（从 date 解析）
  dynasty: string;           // 动态判断的朝代
  
  // 游戏实际数据（动态获取）
  currentEmperor: string | null;        // 当前皇帝名字
  currentEmperorTitle: string | null;   // 皇帝头衔
  currentEraName: string | null;        // 年号
  
  // 真实历史参考
  historicalReferenceInfo: {
    period: string;            // 历史时期名称
    context: string;           // 时代背景描述
    notableEvents: string[];   // 重要历史事件
    notableFigures: string[];  // 历史人物
  }
}
```

### 2. 核心方法

#### parseHistoricalContext()
**调用时机**：GameData 初始化时自动调用

**功能**：
- 从 `date` 字符串中提取年份（如 "976年5月3日" → 976）
- 根据年份判断朝代（唐/五代/北宋/南宋/元）
- 调用 `getHistoricalReferenceByYear()` 获取历史背景
- 初始化当前皇帝信息为 null（等待 characters 加载后更新）

```javascript
parseHistoricalContext() {
  const yearMatch = this.date.match(/(\d+)年/);
  this.year = yearMatch ? parseInt(yearMatch[1]) : 976;
  
  if (this.year < 907) {
    this.dynasty = '唐朝';
  } else if (this.year < 960) {
    this.dynasty = '五代十国';
  } else if (this.year < 1127) {
    this.dynasty = '北宋';
  } else if (this.year < 1279) {
    this.dynasty = '南宋';
  } else {
    this.dynasty = '元朝';
  }
  
  this.currentEmperor = null;
  this.currentEmperorTitle = null;
  this.currentEraName = null;
  
  this.historicalReferenceInfo = this.getHistoricalReferenceByYear(this.year);
}
```

#### getHistoricalReferenceByYear(year)
**功能**：返回真实历史的参考信息

**时间覆盖范围**：
- 875-907：唐末黄巢起义至唐朝灭亡
- 907-960：五代十国
- 960-976：北宋开国
- 976-1000：北宋统一战争
- 1000-1022：咸平景德年间（澶渊之盟）
- 1022-1050：仁宗前期
- 1050-1063：仁宗后期（庆历新政）
- 1063-1085：熙宁变法
- 1085-1100：元祐更化
- 1100-1126：北宋末期
- 1126-1142：靖康之变与南宋建立
- 1142-1162：绍兴议和
- 1162-1189：孝宗中兴
- 1189-1234：南宋中期
- 1234-1260：宋蒙战争前期
- 1260-1279：南宋末期
- 1279+：元朝建立

**返回格式**：
```javascript
{
  period: '历史时期名称',
  context: '时代背景描述',
  notableEvents: ['事件1', '事件2'],
  notableFigures: ['人物1', '人物2']
}
```

#### updateCurrentEmperorInfo()
**调用时机**：`loadCharactersSummaries()` 中，在 characters 数据加载完成后

**功能**：
- 遍历所有角色，查找 primaryTitle 包含"皇帝"/"天子"/"陛下"的角色
- 优先检查玩家是否是皇帝（支持玩家篡位）
- 提取皇帝名字和头衔
- 尝试从头衔中提取年号（如 "宋淳祐皇帝" → "淳祐"）

```javascript
updateCurrentEmperorInfo() {
  for (const char of this.characters.values()) {
    if (char.primaryTitle && (
      char.primaryTitle.includes('皇帝') || 
      char.primaryTitle.includes('天子') ||
      char.primaryTitle.includes('陛下')
    )) {
      this.currentEmperor = char.shortName;
      this.currentEmperorTitle = char.primaryTitle;
      break;
    }
  }
  
  const player = this.getPlayer();
  if (player && player.primaryTitle && (
    player.primaryTitle.includes('皇帝') || 
    player.primaryTitle.includes('天子')
  )) {
    this.currentEmperor = player.shortName;
    this.currentEmperorTitle = player.primaryTitle;
  }
  
  if (this.currentEmperorTitle) {
    const eraMatch = this.currentEmperorTitle.match(/宋(.+?)皇帝/);
    if (eraMatch) {
      this.currentEraName = eraMatch[1];
    }
  }
}
```

---

## 📝 模板使用指南

### 推荐模板：`推荐模板_动态历史版.hbs`

#### 模板结构

```handlebars
═══════════════════════════════════════════════════════
    {{gameData.dynasty}}宫廷对话系统 - 动态历史版
═══════════════════════════════════════════════════════

【当前时空】
时间：{{gameData.date}}（{{gameData.year}}年）
朝代：{{gameData.dynasty}}
{{#if gameData.currentEmperor}}
在位：{{gameData.currentEmperorTitle}} {{gameData.currentEmperor}}
{{#if gameData.currentEraName}}年号：{{gameData.currentEraName}}{{/if}}
{{else}}
当前政局：（从游戏角色信息中动态获取）
{{/if}}

【真实历史参考】
历史时期：{{gameData.historicalReferenceInfo.period}}
时代特征：{{gameData.historicalReferenceInfo.context}}
{{#if gameData.historicalReferenceInfo.notableEvents}}
历史事件：{{#each gameData.historicalReferenceInfo.notableEvents}}{{this}}{{#unless @last}}、{{/unless}}{{/each}}
{{/if}}
{{#if gameData.historicalReferenceInfo.notableFigures}}
历史人物：{{#each gameData.historicalReferenceInfo.notableFigures}}{{this}}{{#unless @last}}、{{/unless}}{{/each}}
{{/if}}

═══════════════════════════════════════════════════════
            历史认知边界 - 核心规则
═══════════════════════════════════════════════════════

你是 {{character.shortName}}，生活在{{gameData.year}}年。

【可知范围】
✓ {{gameData.year}}年及之前的历史事件
✓ 你的身份能接触到的信息
✓ 当前社会流传的消息
✓ 真实历史人物（即使游戏剧情改变，范仲淹、岳飞等历史人物仍作为历史知识存在）

【不可知范围】
✗ {{gameData.year}}年之后的任何事
✗ 未来的皇帝更替
✗ 后世的历史评价
✗ 事件的最终结局

【重要说明】
游戏中历史可能与真实历史不同（玩家可能篡位、改变历史）。
你应该称呼游戏中实际的皇帝和官员，但可以谈论真实历史人物作为背景知识。

【叙事视角】
✓ 使用第一人称角色视角
✓ 表达不确定性："未来如何尚难预料"
✗ 禁用史书视角："历史证明...""后来发生..."
✗ 禁止预知未来："将来会...""最终导致..."
```

#### 可用变量说明

| 变量 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `gameData.year` | number | 当前年份 | 976 |
| `gameData.dynasty` | string | 朝代 | "北宋" |
| `gameData.date` | string | 完整日期 | "976年5月3日" |
| `gameData.currentEmperor` | string \| null | 当前皇帝名字 | "赵光义" |
| `gameData.currentEmperorTitle` | string \| null | 皇帝头衔 | "宋太宗皇帝" |
| `gameData.currentEraName` | string \| null | 年号 | "太平兴国" |
| `gameData.historicalReferenceInfo.period` | string | 历史时期 | "北宋统一战争" |
| `gameData.historicalReferenceInfo.context` | string | 时代背景 | "继续统一战争..." |
| `gameData.historicalReferenceInfo.notableEvents` | string[] | 历史事件列表 | ["灭北汉", "高梁河之战"] |
| `gameData.historicalReferenceInfo.notableFigures` | string[] | 历史人物列表 | ["赵普", "潘美", "杨业"] |

---

## 🔧 如何修改系统

### 1. 修改年份范围

**位置**：`main.js` → `parseHistoricalContext()` 方法

```javascript
// 修改朝代判断逻辑
if (this.year < 907) {
  this.dynasty = '唐朝';
} else if (this.year < 960) {
  this.dynasty = '五代十国';
}
// 添加更多朝代...
```

### 2. 添加新的历史时期

**位置**：`main.js` → `getHistoricalReferenceByYear()` 方法

```javascript
else if (year >= 1189 && year < 1234) {
  return {
    period: '南宋中期',
    context: '宋金对峙，蒙古崛起改变局势',
    notableEvents: ['开禧北伐', '蒙古西征', '宋蒙联合灭金'],
    notableFigures: ['韩侂胄', '史弥远', '辛弃疾', '陆游']
  };
}
```

**字段说明**：
- `period`：时期名称（简短）
- `context`：时代背景（一句话概括）
- `notableEvents`：重要事件（数组）
- `notableFigures`：历史人物（数组）

### 3. 调整皇帝识别逻辑

**位置**：`main.js` → `updateCurrentEmperorInfo()` 方法

```javascript
// 修改识别关键词
if (char.primaryTitle && (
  char.primaryTitle.includes('皇帝') || 
  char.primaryTitle.includes('天子') ||
  char.primaryTitle.includes('可汗')  // 添加新关键词
)) {
  // ...
}
```

### 4. 自定义模板

**位置**：`推荐模板_动态历史版.hbs`

- 修改排版格式
- 添加更多 gameData 变量
- 调整提示词内容
- 更改历史认知规则

**注意**：修改后需要在游戏设置中重新加载模板

---

## 🧪 测试方法

### 测试场景 1：正常历史进程

1. 开始 976 年游戏
2. 观察 AI 对话是否提到：
   - 当前年份：976 年
   - 朝代：北宋
   - 可能提到：赵普、潘美、杨业
   - 不应提到：范仲淹（还未出生）、岳飞（未来人物）

### 测试场景 2：玩家篡位

1. 玩家通过某种方式成为皇帝
2. 观察 AI 是否：
   - 在 `currentEmperor` 中显示玩家名字
   - 对话中称呼玩家为皇帝
   - 仍然能谈论真实历史人物（如范仲淹）

### 测试场景 3：跨时代游戏

1. 分别在以下年份测试：
   - 880 年（唐末）→ 应提到黄巢起义
   - 920 年（五代）→ 应提到五代十国
   - 1040 年（北宋中期）→ 应提到庆历新政
   - 1130 年（南宋初期）→ 应提到靖康之变、岳飞
   - 1270 年（南宋末期）→ 应提到蒙古南下

### 测试场景 4：认知边界

1. 在 1000 年的游戏中询问 AI：
   - "范仲淹是谁？" → 不应认识（范仲淹 989-1052，1000 年时才 11 岁且未出名）
   - "澶渊之盟如何？" → 不应知道（澶渊之盟发生在 1004 年）
   - "未来会怎样？" → 应回答不确定、无法预知

### 调试方法

在浏览器开发者工具中查看 gameData：

```javascript
// 在控制台输入
console.log(currentConversation.gameData);

// 应该看到：
{
  year: 976,
  dynasty: "北宋",
  currentEmperor: "赵光义",
  currentEmperorTitle: "宋太宗皇帝",
  currentEraName: "太平兴国",
  historicalReferenceInfo: {
    period: "北宋统一战争",
    context: "继续统一战争...",
    notableEvents: ["灭北汉", "高梁河之战", "雍熙北伐"],
    notableFigures: ["赵普", "潘美", "杨业"]
  }
}
```

---

## 📊 工作流程图

```
游戏开始
  ↓
解析日志 → 获取 date 字符串
  ↓
parseHistoricalContext()
  ├─ 提取年份 → this.year
  ├─ 判断朝代 → this.dynasty
  └─ 获取历史背景 → this.historicalReferenceInfo
  ↓
等待角色数据加载
  ↓
loadCharactersSummaries()
  ├─ updateCurrentEmperorInfo()
  │   ├─ 查找游戏中的皇帝
  │   ├─ 优先检查玩家
  │   └─ 提取年号
  └─ 加载对话摘要
  ↓
生成 AI 提示词
  ├─ 渲染 Handlebars 模板
  ├─ 填充 gameData 变量
  └─ 包含历史认知规则
  ↓
AI 生成回复（符合时代背景，不预知未来）
```

---

## ⚠️ 注意事项

### 1. 数据依赖

- **年份提取**：依赖 `date` 字符串格式为 "XXX年X月X日"
- **皇帝识别**：依赖 `primaryTitle` 包含关键词
- **调用顺序**：必须先加载 characters 再调用 `updateCurrentEmperorInfo()`

### 2. 性能考虑

- `parseHistoricalContext()` 只在初始化时调用一次
- `getHistoricalReferenceByYear()` 使用简单的 if-else，性能优秀
- `updateCurrentEmperorInfo()` 遍历所有角色，性能可接受

### 3. 边界情况

- **未找到皇帝**：`currentEmperor` 为 null，模板会显示"从游戏角色信息中动态获取"
- **年份早于 875**：返回默认的"唐朝时期"
- **年份晚于 1279**：返回"元朝建立"
- **日期格式异常**：默认年份为 976

### 4. 多语言支持

目前系统使用中文硬编码，如需支持其他语言：
- 修改 `getHistoricalReferenceByYear()` 返回多语言对象
- 修改模板使用语言变量
- 添加翻译文件

---

## 🔄 版本历史

### v6.1 (当前版本)
- ✅ **全面中文化**：将所有提示词系统改为中文
  - 主提示词（整合动态历史+道德审查解除）
  - 对话摘要系统（标题、时间描述）
  - 记忆系统（模板）
  - 滚动摘要（模板）
  - 主指令（对话和信件）
  - 错误信息
- ✅ **整合道德审查解除指令**到主提示词
- ✅ **统一语言风格**：确保整个系统语言一致性

### v6.0
- ✅ 实现动态历史认知系统
- ✅ 添加年份和朝代自动判断
- ✅ 从游戏数据动态获取当前皇帝
- ✅ 提供真实历史参考信息
- ✅ 扩展时间范围：875-1279 年
- ✅ 创建推荐模板和完整文档
- ✅ 删除旧版本说明文档

### 之前版本
- v5.2.2：修复缓存冲突 bug
- v5.2.1：修复第三方角色记忆加载
- v5.2：修复官职变化导致重复文件夹
- v5.1：智能缓存机制
- v5.0：动态角色数据加载

---

## 🎯 未来改进方向

### 短期
- [ ] 添加更细粒度的历史时期（按月）
- [ ] 支持更多朝代和历史时期
- [ ] 优化年号提取算法

### 长期
- [ ] 多语言支持
- [ ] 可视化历史时间轴
- [ ] 自定义历史事件编辑器
- [ ] 历史事件触发系统

---

## 📞 常见问题

### Q1: AI 提到了未来的历史事件怎么办？

**A**: 检查以下几点：
1. 模板中的"历史认知边界"规则是否正确加载
2. AI 使用的模型是否理解中文提示词
3. 是否需要加强"禁止预知未来"的规则表述

### Q2: 系统无法识别游戏中的皇帝

**A**: 检查：
1. `primaryTitle` 中是否包含"皇帝"/"天子"/"陛下"关键词
2. 如果使用其他语言，需要修改 `updateCurrentEmperorInfo()` 中的关键词
3. 使用控制台查看 `characters` 数据是否正确加载

### Q3: 如何添加新的朝代？

**A**: 
1. 修改 `parseHistoricalContext()` 添加朝代判断
2. 修改 `getHistoricalReferenceByYear()` 添加对应时期
3. 确保年份范围不重叠

### Q4: 历史人物列表太长/太短

**A**: 
- 在 `getHistoricalReferenceByYear()` 中调整 `notableFigures` 数组
- 建议每个时期 3-6 个代表人物
- 优先选择知名度高、影响力大的人物

---

## 📚 相关文件清单

### 核心文件
- `resources/app/out/main/main.js` - 主程序逻辑
- `推荐模板_动态历史版.hbs` - 推荐使用的提示词模板
- `动态历史认知系统_完整文档.md` - 本文档

### 已删除文件（旧版本）
- ~~v5.2_修复官职变化bug.md~~
- ~~其他版本说明文档~~

---

## 🤝 贡献指南

如果您想改进此系统：

1. **添加历史时期**：参考现有格式，确保信息准确
2. **优化识别逻辑**：提高皇帝、年号识别准确率
3. **改进模板**：创建更好的提示词结构
4. **报告问题**：发现 bug 请详细描述场景和复现步骤

---

**文档版本**: v1.1  
**最后更新**: 2026-08-11  
**适用系统版本**: v6.1+
