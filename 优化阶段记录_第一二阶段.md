# VOTC 动态历史与缓存优化记录（第一、第二阶段）

> 记录日期：2026-08-13  
> 项目：Voices of the Court（CK3 Mod / Electron 客户端）  
> 目的：为后续优化提供可直接续接的技术背景，避免重复分析已经完成的工作。

## 一、优化目标与约束

本轮优化围绕三个目标展开：

1. 让动态历史系统遵守 CK3 当前游戏年份，避免人物、诗词、事件和作品“穿越式认知”。
2. 提升 DeepSeek 等模型的提示词缓存命中率，减少未命中 Token 和 API 成本。
3. 限制动作模型调用，只在对话中出现明确动作/状态变化时触发。

实现时保留了原有的记忆摘要、历史摘要、人物上下文和动作执行流程，没有重写摘要系统或历史数据结构。

## 二、第一阶段：统计、定位与基线

### 2.1 使用量统计

在主进程增加 `UsageAnalytics`，统一记录每次 LLM 请求的轻量元数据和用量结果：

- 请求类型：`chat`、`action`、各类摘要、信件等。
- provider、模型、角色、动作触发原因。
- 估算输入 Token、实际输入/输出/总 Token。
- DeepSeek 返回的缓存命中 Token 与未命中 Token。
- 提示词分块 Token 统计，用于定位最占用上下文的区块。
- 动作模型被跳过时记录 `action_skipped` 和跳过原因。

默认只保留最近 2000 条记录，不保存提示词正文、回复正文或 API Key。

运行时文件：

```text
C:\Users\97330\AppData\Roaming\VOTC\votc_data\usage-analytics.json
```

主进程接口：

- `usage:getReport`：读取总量、按请求类型汇总、按提示词区块汇总和最近记录。
- `usage:clear`：清空统计记录。

预加载接口：

```js
window.usageAPI.getReport()
window.usageAPI.clear()
```

### 2.2 第一阶段定位结论

统计显示，主要成本来自重复发送的大段动态上下文，尤其是角色信息、历史/记忆内容、动作可用列表和每轮变化的提示词尾部。动作模型在普通对话中无条件调用，会制造大量低价值请求和未命中缓存。

## 三、第二阶段：缓存稳定化与动作调用门控

### 3.1 稳定缓存锚点

在普通聊天提示词最前面加入固定系统消息：

```text
VOTC_CACHE_ANCHOR_v1
```

该锚点只包含长期稳定的角色扮演、历史边界和输出约束，不包含当前日期、角色名、对话内容或动态数值。动态上下文仍按原有顺序追加在锚点之后，因此不会破坏记忆摘要和历史系统。

动作模型使用独立的固定锚点：

```text
VOTC_ACTION_CACHE_ANCHOR_v1
```

普通聊天和动作提示词分别保持自己的缓存前缀，避免两种任务互相污染缓存。

### 3.2 历史年代边界

系统提示词和推荐模板中加入“年代核验”规则：

- 先读取当前游戏年份，再判断人物、事件、作品和诗词是否已经存在。
- 当前年份早于对象出现年份时，不得表现为已知事实。
- 不确定时明确说明无法确认，不强行补全。
- 角色可以讨论当时已经存在的传闻、先秦典籍或本时代知识，但不能使用后世作品和现代解释。

典型校验：若当前年份为 1013 年，辛弃疾及其《丑奴儿·书博山道中壁》尚未出现，角色不能说“听过”或引用“却道天凉好个秋”。

相关提示词文件：

- `resources/app/default_userdata/prompts/system/default.hbs`
- `推荐模板_动态历史版.hbs`

### 3.3 动作模型触发门控

动作模型现在只接受一个明确的候选消息，不再扫描最近三条历史消息：

- 玩家新消息含有已发生动作时，本轮第一个 NPC 使用该玩家消息进行一次动作判定。
- 后续 NPC 只检查自己刚生成的回复。
- 同一轮内同一种动作类别只触发一次，避免同一事件被多个 NPC 重复请求。
- 没有明确状态变化时只记录 `action_skipped`，不调用动作 API。

已覆盖的动作类别包括：

- 金钱支付、收取、赠与。
- 囚禁、逮捕、释放。
- 死亡、受伤、处决等结果。
- 关系、任职、信仰、效忠变化。
- 离开、移动、退出场景。
- 饮茶、饮酒、举杯、敬酒。
- 拔剑、交战、攻击、格挡、击败等战斗动作。
- 脱衣、解带等亲密场景动作。
- 行房、云雨、交合、欢好、同房、圆房、事毕后相拥等明确完成表述。

同时加入了未来计划过滤，例如“准备”“打算”“明日”“待会”“计划”等未发生动作不会触发；普通“已经回到座位”这类句子也不会被误判为性行为。

## 四、第二阶段涉及的代码位置

核心文件：

| 文件 | 用途 |
| --- | --- |
| `resources/app/out/main/main.js` | 统计、缓存锚点、LLM 用量追踪、动作触发门控、会话逻辑 |
| `resources/app/out/preload/preload.js` | 暴露 `window.usageAPI` |
| `resources/app/default_userdata/prompts/system/default.hbs` | 默认系统提示词与历史年代规则 |
| `推荐模板_动态历史版.hbs` | 动态历史模板与年代核验说明 |
| `README.md` | 项目使用、统计和缓存说明 |

关键实现入口（当前打包版本）：

- `UsageAnalytics`：约第 1125 行。
- `LLMManager.sendChatRequest/sendActionsRequest`：约第 1272 行。
- `PromptBuilder.buildCacheAnchor`：约第 3599 行。
- `PromptBuilder.buildMessagesWithTokenCount`：约第 3845 行。
- `ActionPromptBuilder`：约第 4909 行。
- `ActionEngine.getActionTrigger/shouldEvaluateForMessage`：约第 5280 行。
- `Conversation.respondAs/sendMessage`：约第 5711、5982 行。

行号会随着后续打包或编辑变化，应优先按函数名搜索。

## 五、实测结果

根据 DeepSeek 控制台截图，对比优化前后的单日数据：

| 指标 | 2026-08-12（优化前） | 2026-08-13（优化后） | 变化 |
| --- | ---: | ---: | ---: |
| 输入 Token | 6,117,719 | 2,685,042 | ↓ 56.1% |
| 缓存命中 Token | 3,920,128 | 2,114,944 | — |
| 缓存未命中 Token | 2,197,591 | 570,098 | ↓ 74.1% |
| 缓存命中率 | 64.08% | 78.77% | ↑ 14.69 个百分点 |
| 总 Token | 6,542,303 | 2,816,857 | ↓ 56.9% |
| 输出 Token | 424,584 | 131,815 | ↓ 68.9% |

结论：稳定前缀和动作门控已显著减少未命中缓存及无效动作请求。由于两天对话量不同，后续应继续观察多日平均值，不应只用单日总量判断效果。

## 六、后续优化入口

下一阶段建议按以下顺序推进：

1. 连续收集 3～7 天统计，按 `requestType` 和提示词区块比较缓存命中率。
2. 重点检查 `blocks` 中长期高 Token 的动态区块，评估是否能进一步压缩或拆分。
3. 检查动作触发日志中的 `skipReason`，统计误触发、漏触发和同轮重复情况。
4. 在不改变摘要/历史数据结构的前提下，考虑对稳定角色资料做更细的缓存分层。
5. 每次优化后保留一段“优化前/后”对照数据，避免只凭主观体验调整。

## 七、验证与注意事项

每次修改 `main.js` 后至少执行：

```powershell
node --check resources\app\out\main\main.js
git diff --check
```

动作触发回归应覆盖：普通对话、未来计划、饮茶/饮酒、战斗、已完成亲密行为，以及同一轮多个 NPC 的重复触发。不要删除或重建 `usage-analytics.json`，除非明确需要清空统计；清空前应先备份。

