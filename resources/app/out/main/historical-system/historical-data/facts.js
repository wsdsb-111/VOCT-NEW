"use strict";

// V8.0 defines and validates the fact contract. Curated fact seeding begins in
// the v8.3-v8.5 phases so migration does not invent data absent from legacy.
const facts = [];

module.exports = { facts };
