"use strict";

// Ruler resolution is introduced in v8.2. V8.0 keeps this dataset empty while
// establishing a validated schema and compatibility boundary.
const rulers = [];

module.exports = { rulers };
