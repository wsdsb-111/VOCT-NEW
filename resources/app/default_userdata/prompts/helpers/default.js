// Default helpers file. Export a function that receives Handlebars and registers helpers.
module.exports = (Handlebars) => {
  // Add custom helpers here if needed.
  console.log('Default helpers loaded');
};
